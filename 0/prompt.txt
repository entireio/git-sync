fix

---

Arroz del senyoret

---

Found one issue in PR #120 at f68a4fd:

  - [P2] Source truncation can be mislabeled as expected interruption — git-sync/internal/strategy/bootstrap/
    pack_observer.go:173. sourceEOF only records io.EOF. If the source returns io.ErrUnexpectedEOF, Close()
    substitutes errObservationInterrupted, producing observer_failed=false and observer_interrupted=true. I
    reproduced this with a valid pack prefix followed by io.ErrUnexpectedEOF. The upload still fails, but the new
    diagnostics hide source truncation. Preserve source-read errors when closing the observer pipe and add
    coverage for this case.

  No other findings in fallback enforcement or retry classification. Relevant package tests and bootstrap/
  transport race tests passed; all four CI checks remain green. The isolated reproduction fails as described. No
  repository files changed.

---

what's the status here?

---

can you fix it?