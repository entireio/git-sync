- P3: The mixed flag/positional bug is still present in cmd/git-sync/convert_sha256.go:45. --source-url <url> <target-dir> still leaves TargetDir empty because the code only reads args[1]. I confirmed it with:
    go run ./cmd/git-sync convert-sha256 --source-url http://example.invalid/repo.git /tmp/git-sync-review-out
    It exits with convert-sha256 requires a source URL and a target directory.